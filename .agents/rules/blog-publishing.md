# Blog Publishing Policy

## Headline Management
- When a new blog entry is published, only the single latest new entry occupies the headline block at the top of the blog section on `index.html`.
- The previous headline entry MUST be relegated to the archive section (`js/blog-data.js` and the native archive grid).
- Never leave old headline entries sitting as duplicate or lingering featured cards on the main page.
